<h1>All Users</h1>

<ul>
<% @users.each do |user| %>
<li><%= user.username %>
	<% if Friendship.can_friend?(user.id, current_user.id) %>
		<%= button_to "Add Friend", user_friendship_url(user.id), :method => :post %>
	<% end %>
	</li>
<% end %>
</ul>
