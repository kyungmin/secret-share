<h1><%= @user.username %></h1>

<h2>Secrets</h2>
<ul id="secrets">
  <% @user.received_secrets.each do |secret| %>
    <li><%= secret.title %></li>
  <% end %>
</ul>

<%= render 'secrets/form', user_id: @user_id %>